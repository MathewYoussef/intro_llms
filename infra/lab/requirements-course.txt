# Course-level dependencies (kept intentionally small).
# Prefer using what's already inside the base `isaac-gr00t:*jupyterlab*` image.

# Experiment hygiene
python-dotenv
rich

# Core HF stack helpers (often already present, but harmless if redundant)
accelerate
datasets
evaluate
peft
trl

# Nemotron runtime deps (required by the model's custom code)
mamba-ssm
causal-conv1d

# Plotting / analysis
matplotlib
pandas
scikit-learn
